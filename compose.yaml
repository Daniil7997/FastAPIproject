services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - accounts_service
      - resource_service
      - grafana
    networks:
      - app_network

# Подключаем файл сервиса accounts
include:
  - ./services/accounts/compose.yaml
  - ./services/resource/compose.yaml
  - ./deploy/compose.yaml


networks:
  app_network:
    driver: bridge
